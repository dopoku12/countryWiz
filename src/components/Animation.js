import { useEffect, useRef } from "react";
import { gsap } from "gsap";
const Animation = () => {
    comp = useRef();

    useEffect(() => {


        return () => {
        }
    }, []);

}

export default Animation;