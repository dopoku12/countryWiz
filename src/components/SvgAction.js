const SvgAction = (props) => {
    const id = document.getElementById('svgMap')
    console.log('id:', id);
    // const child = id.childNodes
    // console.log(child);
    const cId = props.countryId
    // const child = props.children
    // const svgObj = child.props.children
    // console.log('child:', svgObj);
    // const svgId = svgObj.map((i) => i.props).map((i) => i.id)
    // console.log('id:', svgId);

    // svgId.map(i => {

    // }
    // )


}

export default SvgAction;