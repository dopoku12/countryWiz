const Svg = (props) => {
    // const cl = document.getElementById('svgMap');
    // console.log('cl', cl);
    // const clNode = cl.childNodes;
    // console.log('childNodes:', clNode);
    console.log(props.children)
}

export default Svg;